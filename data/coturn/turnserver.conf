listening-port=3478
tls-listening-port=5349

# SERVER INTERNAL IP - EXTERNAL PUBLIC IP
# If you're NAT'ing the whole server
# external-ip=192.168.0.1/1.2.3.4
min-port=49152
max-port=65535
verbose

fingerprint
lt-cred-mech

# Replace 'turn.example.com' by your TURN domain name
realm=turn.example.com

# If you plan to use SSL, uncomment this
# cert=/etc/ssl/certs/cert.pem
# pkey=/etc/ssl/private/privkey.pem

syslog

cli-ip=127.0.0.1
cli-port=5766
cli-password=tHISpASSWORDiSuSELESS-ButChangeItAnyway

# You could use postgresql or mysql, but you first need to
# set the database up, using specific schema files provided
# by the CoTURN developers...
# I prefer using a persistent SQLite database. COTURN only
# use a database to store TURN secrets... So, in our case,
# this will be an entire database to store one login/password.
